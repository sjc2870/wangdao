0 什么是友元？友元的存在形式有？友元有何特点？
	通过友元能使一个函数或者类访问一个类中private的成员。存在形式有友元函数和友元类。
友元不具备传递性，友元是单向的，破坏了传递性。A类的成员函数作为B类的友元函数时，必须先定义A类
而不仅仅是声明它
1、运算符重载的原则是什么？有哪些规则？
	不能对内置类型进行运算符重载，只能对类类型或者枚举类型进行重载。
	操作符的优先级，结合性或操作数个数不能改变
	不再具备短路求值的特性
	不能臆造重载一个不存在的运算符
2、不能重载的运算符有哪几个
	. .* :: sizeof ?:
3、运算符重载的形式有哪几种
	3种。 1 定义为普通函数 2 定义为友元函数  3 定义为成员函数
4、自增运算符的前置形式和后置形式有哪些区别？返回值类型分别是什么
	前置：type& operator++()   后置 type operator++(int)
