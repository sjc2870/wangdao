1、可分为内核区，栈区，堆区，全局/静态区，字符串常量区，代码区
	内核区：用户不可访问
	栈区：自高地址向低地址增长
	堆区：自低地址向高地址增长
	全局/静态区：存放全局变量和静态变量
	字符串常量区：存放字符串常量
	代码区：存放指令
2、会自动生成构造函数，析构函数，复制构造函数。
	系统默认的构造函数只是执行简单的浅拷贝
	默认析构函数不能回收类里面的堆内存，需要自己实现回收堆内存
	复制构造函数的形式必须是const Computer & com   
		> 不加引用会造成死循环
		> 不加&无法对右值/临时变量执行拷贝复制
3、浅拷贝只是传递地址，无法复制真实内容，尤其在执行成员拥有堆内存时的复制构造时尤其容易出问题！
   深拷贝先开辟空间，然后把内容全部复制过去


